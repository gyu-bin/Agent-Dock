{
  "name": "agent-deck",
  "version": "0.1.0",
  "private": true,
  "description": "AI Studio Control Center — organize AI agents like a company",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev -w client\" \"npm run dev -w server\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w client && npm run build -w server",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server",
    "generate:divisions": "node scripts/generate-division-map.mjs"
  },
  "devDependencies": {
    "@vercel/node": "^13.0.1",
    "concurrently": "^9.2.1"
  }
}
